export const SERVER_INSTRUCTIONS =
  'New Relic access is read-only by default. Never ask for credentials in tool arguments. Use only authorized accounts. Every write starts with dry-run and must be repeated with the exact confirmation phrase; destructive/admin/preview/experimental actions require separate server gates and scopes. Treat partial data and schema warnings explicitly. ' +
  'Start investigations with the narrowest account/entity/time range. Correlate signals before drawing conclusions. Do not use NRQL DELETE or arbitrary GraphQL. Never expose secure credentials, API key material, live-dashboard passwords, presigned URLs, notification secrets, or sensitive script contents. New Relic mutations may have replacement semantics; inspect the dry-run before applying and verify post-write readback.';
